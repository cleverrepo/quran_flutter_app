import 'package:flutter/material.dart';

final  maiColor = Colors.brown.shade600;
final shadowColor = Colors.grey.shade500;
const double  fontSize=14;
final secondaryColor=Colors.white;
const primaryColor=Colors.black;
final shadow =
    BoxShadow(color: shadowColor, blurRadius: 6, offset: const Offset(3, 3));
final secondShadow =
    const BoxShadow(color: Colors.white, blurRadius: 6, offset: Offset(-3, -3));
